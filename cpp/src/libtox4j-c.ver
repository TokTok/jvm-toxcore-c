# Linker script to export only the JNI symbols and symbols needed by
# the test code.
{
  global:
    Java_*;
    JNI_*;
    _Z12scope_indent*;
    _Z29throw_illegal_state_exception*;
    _Z19throw_tox_exception*;
    _Z26throw_tox_killed_exception*;
    _Z17tox4j_fatal_error*;
  local: *;
};
